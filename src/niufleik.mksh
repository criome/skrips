#!/usr/bin/env mksh

set -o errexit

fleikPath=~/dev/$1

mkdir $fleikPath
cd $fleikPath
git init

cat > $fleikPath/flake.nix <<EOF
{
  description = "${1}";

  edition = 201909;

  outputs = { self }: {
    strok = {};
    kor = {};
  };
}
EOF
